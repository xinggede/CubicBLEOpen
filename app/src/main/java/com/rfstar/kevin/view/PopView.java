package com.rfstar.kevin.view;import android.content.Context;import android.graphics.drawable.BitmapDrawable;import android.graphics.drawable.ColorDrawable;import android.view.Gravity;import android.view.View;import android.widget.PopupWindow;import com.rfstar.kevin.R;/** * popview 基类 * * @author kevin */public class PopView {    public Context context = null;    public PopupWindow pop = null;    public View popParent = null; // 显示所依托的View    public PopView(Context context, View parentLayout) {        // TODO Auto-generated constructor stub        this.context = context;        this.popParent = parentLayout;    }    public void dismiss() {        pop.dismiss();    }    @SuppressWarnings("static-access")    public void setPopView() {        pop.setAnimationStyle(R.style.pop_anim_style);        ColorDrawable dw = new ColorDrawable(-00000);        pop.setBackgroundDrawable(dw);        pop.showAtLocation(popParent, Gravity.CENTER_HORIZONTAL, 0, 0); // |        // Gravity.BOTTOM        pop.setOutsideTouchable(false);        pop.update();    }    @SuppressWarnings("deprecation")    public void setPopDownView() {        pop.setAnimationStyle(R.style.pop_anim_style);        pop.setBackgroundDrawable(new BitmapDrawable());        pop.showAtLocation(popParent, Gravity.CENTER_HORIZONTAL                | Gravity.BOTTOM, 0, 0); //        pop.setOutsideTouchable(false);    }    public void setDropDown() {        pop.setAnimationStyle(R.style.pop_anim_style);        pop.setBackgroundDrawable(new BitmapDrawable());        pop.showAsDropDown(popParent, 0, 0);        pop.setOutsideTouchable(false);    }}